// DataCards/config.js

// Optional, default container selector or other config here
export const DEFAULTS = {
  container: '#dataCardsContainer',
  // could add formatting preferences here
};
